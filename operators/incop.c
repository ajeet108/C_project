#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
	//int i=5;
	//i=i++;
	//;
	//printf(" i= %d\n",i);
	//const int x = 5;
	//int x=5;
	//x++ + ++x;
	//printf("%d\n", x++ + ++x);  // Undefined behavior
	/*
    	int array[] = {1, 2, 3, 4, 5};
    	int *ptr = array;  // ptr points to the first element of the array

    	// Using increment operator on the pointer
    	printf("Original value: %d\n", *ptr);  // Output: 1
    	ptr++;  // Move the pointer to the next element
    	printf("After increment: %d\n", *ptr);  // Output: 2
	*/
	int a = 10, b = 10;

        if (a = 5)

        b--;

        printf("%d, %d\n", a, b--);
    	return 0;
}
